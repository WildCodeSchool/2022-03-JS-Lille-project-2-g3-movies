import styled from "styled-components";

export default styled.main`
  .result {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
  }
  h2 {
    text-align: center;
    margin: 2rem;
  }
`;
