import styled from "styled-components";

export default styled.section`
  height: 20vh;
  max-width: 800px;

  padding: var(--section-spacing);
  margin: auto;
  margin-top: 0;
  margin-bottom: 0;
`;
